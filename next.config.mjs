/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  output: "export",
  basePath: "/Portofolio-SidikWaluya",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
